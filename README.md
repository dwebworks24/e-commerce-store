# e-commerce-store
env activate
-------------
1.pip install virtualenv
2.python -m venv env-store
3.env-store\Scripts\activate


pip freeze > requirements.txt 
pip install -r requirements.txt

# .gitignore
echo > .gitignore
